[defaults]
inventory = inventory/hosts.ini
# The role IS this repo; expose its parent so the folder name resolves as a role.
roles_path = ..
host_key_checking = False

[privilege_escalation]
become = True
